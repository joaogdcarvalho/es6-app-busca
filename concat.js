export function bomDia (name) {
  return `Bom dia ${name}!`;
};

export function boaTarde (name) {
  return `Boa tarde ${name}!`;
};

export function boaNoite (name) {
  return `Boa noite ${name}!`;
};

export function confirmacaoSaida (name) {
  return `Tem certeza de que deseja sair ${name}?`;
};